name: Trigger GitHub Actions CI
on:
  push:
    branches:
      - '*'
jobs:
  call-gh-actions:
    uses: sqldropper/github-actions/.github/workflows/parent-ci.yml@master
    with:
      repo-url: ${{ github.event.inputs.repo-url }}
      branch: ${{ github.event.inputs.branch }}